# 2026-09-04 工作记录

## 轻盈计划 v1.3.1 头部改版 + 缓存刷新

用户反馈三个问题：
1. 网页还是旧版（建档页有示例复选框、饮食记录是旧表单）
2. 头部排版需按图调整

诊断：GitHub Pages CDN 缓存 + Service Worker 缓存导致用户看到旧版。

已做修改：
- mast header：删掉 app 标题，换成 EAT WELL · MOVE DAILY · KEEP LIGHT 标语
- greet 区域：删掉 kicker 和早安问候 h1，减少 padding-top
- rHeader()：移除动态问候语 JS
- 加 cache-busting：manifest/icons 链接加 ?v=1.3.1
- sw.js 缓存名改为 flt-v1.3.1
- footer 版本号改为 v1.3.1
- 推送到 GitHub Pages

注意：用户可能需要强制刷新（Ctrl+F5）或清除浏览器缓存才能看到更新，因为 Service Worker 和 Pages CDN 都有缓存。

## v1.4 角色系统上线

用户反馈："其他人不要看到后台设置，这个有份角色吗？"

加上了 admin/member 角色：
- profile 加 role 字段
- 首位建档者自动 admin，后续添加的为 member
- 设置页按角色分块显示：
  * adminOnly：云端同步 + 高级设置（数据库配置）
  * adminOnlyDanger：清理他人 + 清空全部
- 普通成员只看到：个人资料、外观、导出/导入、清空本人数据
- 加 openRoleMgr：管理员可提升/降级成员（至少保留一个 admin）
- 加 wipeSelf：所有角色都能清空自己的数据
- wipeAll/keepOnlyCurrent 加 admin 权限检查防绕过
- 删除建档副标题

推送 v1.4 上线，链接不变。

