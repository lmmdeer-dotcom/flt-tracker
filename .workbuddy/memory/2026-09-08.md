# 2026-09-08

## 回退 v1.9.6 → v1.9.5（已发）

**commit `dd72de9`（Revert "v1.9.6: …严格对齐"）→ origin/main**

- 用户反馈「退回到上一步吧」，确认目标是退回 v1.9.5（撤销 v1.9.6 成员选择器与 Hello 标题顶部严格对齐）
- 处理：`git revert --no-edit 031a671`（保留历史，不改写远端）
- revert 后 index.html / sw.js / 镜像与 v1.9.5（1408b4d）内容完全一致（diff 仅剩 memory 日志）
- 校验：node --check ✅ / 镜像 diff ✅ / push 一次成功
- 版本号回落：footer v1.9.5 / `?v=1.9.5` ×3 / sw.js `flt-v1.9.5`

### 经验
- 「退回上一步」先问清楚回退到哪个版本（AskUserQuestion 给出 v1.9.5 / v1.9.4 / v1.9.3 三档），避免盲改
- revert 比 reset --hard + force push 安全（已推送历史，用 revert 留痕）
- 教训：node --check 用 Git Bash `/tmp` 路径会因 Windows 路径映射报 MODULE_NOT_FOUND，临时文件须写在项目目录内再删
